import streamlit as st
import pandas as pd
from streamlit_gsheets import GSheetsConnection
from datetime import datetime
import io

# --- 1. ตั้งค่าและข้อมูลเบื้องต้น ---

# ข้อมูลดิบตั้งต้น (ใช้กรณีเปิด Tab เดือนใหม่ครั้งแรก)
INITIAL_CSV = """Date,รายการ,ราคา,จำนวน/ชิ้น,รวม/บาท
2026-01-03,เค้กนมสด,50,47,2350
2026-01-03,เค้กญี่ปุ่น,60,12,720
2026-01-03,เค้ก 60 บาท,90,6,540
2026-01-02,ชีสเค้ก,60,0,0
2026-01-02,คุกกี้ป๊อป,25,0,0
2026-01-02,เค้กป๊อป,75,0,0
2026-01-02,มาการองการ์ตูน,70,5,350
2026-01-02,คอนเฟลค,100,0,0
2026-01-02,คอนเฟลค,100,0,0
2026-01-02,ช็อคบอล 30,30,10,300
2026-01-02,ช็อคอล 20,0,0,0
2026-01-02,ชาชงสด,40,0,0
2026-01-02,cold brew,65,0,0
2026-01-02,ลาบูบู้,159,0,0
2026-01-02,คุกกี้เนย,39,0,0
2002-01-21,เมอแรง,35,0,0
2026-01-02,กล่องเค้ก 4,35,0,0
2026-01-02,กล่องเค้ก 12ชี้น,65,0,0
2026-01-02,กล่องเค้ก 24,120,0,0
2026-01-02,กล่องเค้ก 36,100,0,0
2026-01-02,พริกทอด,110,0,0
2026-01-02,เจ้าหญิง/แคร์แบร์,79,0,0
2026-01-02,ถุงผ้า nami,40,0,0
2026-01-02,แก้วน้ำนามิ,0,0,0
2026-01-02,คอนเฟลค,100,0,0
2026-01-02,อื่นๆ,0,0,0
2026-01-02,cake pop,65,0,0
2026-01-02,กะบอกน๋า,0,0,0
2026-01-02,cake orange,45,0,0"""

def get_current_sheet_name():
    """สร้างชื่อ Sheet ตามเดือนปัจจุบัน เช่น Jan_2026"""
    return datetime.now().strftime("%b_%Y")

# สร้างการเชื่อมต่อกับ Google Sheets
conn = st.connection("gsheets", type=GSheetsConnection)

def load_data():
    """โหลดข้อมูลจาก Google Sheets"""
    current_sheet = get_current_sheet_name()
    
    try:
        # ลองอ่านข้อมูลจาก Tab เดือนปัจจุบัน
        df = conn.read(worksheet=current_sheet)
        # ถ้าอ่านได้แต่ข้อมูลว่างเปล่า (Empty DataFrame)
        if df.empty:
             raise ValueError("Empty sheet")
        
        # แปลง format วันที่
        df['Date'] = pd.to_datetime(df['Date']).dt.date
        return df
        
    except Exception:
        # กรณี 1: ยังไม่มี Tab ของเดือนนี้ (เช่น เปลี่ยนเดือนใหม่)
        # กรณี 2: เพิ่งสร้างไฟล์ครั้งแรก
        # ให้โหลดข้อมูลตั้งต้น (Template) มาใช้
        df = pd.read_csv(io.StringIO(INITIAL_CSV))
        df['Date'] = pd.to_datetime(df['Date'], errors='coerce').dt.date
        df = df.fillna(0)
        return df

# --- 2. ส่วนหน้าจอแสดงผล (Streamlit UI) ---

st.set_page_config(page_title="ระบบเบิกของ Cloud", layout="wide")

st.title("☁️ ระบบเบิกของหน้าร้าน (Google Sheets)")
st.caption(f"Sheet ประจำเดือน: {get_current_sheet_name()}")

# โหลดข้อมูลเข้า Session State
if 'df' not in st.session_state:
    st.session_state.df = load_data()

# ส่วนแสดงผลสรุปยอด (Metrics)
st.subheader("📊 สรุปยอดขาย")
col1, col2, col3 = st.columns(3)

current_df = st.session_state.df
today = datetime.now().date()

# คำนวณยอด
daily_sales = current_df[current_df['Date'] == today]['รวม/บาท'].sum() if 'รวม/บาท' in current_df.columns else 0
monthly_sales = current_df['รวม/บาท'].sum() if 'รวม/บาท' in current_df.columns else 0
items_today = current_df[current_df['Date'] == today]['จำนวน/ชิ้น'].sum() if 'จำนวน/ชิ้น' in current_df.columns else 0

col1.metric("ยอดรวมวันนี้ (บาท)", f"{daily_sales:,.2f}")
col2.metric("ยอดรวมเดือนนี้ (บาท)", f"{monthly_sales:,.2f}")
col3.metric("จำนวนชิ้นที่ออกวันนี้", f"{items_today:,.0f} ชิ้น")

st.divider()

# ส่วนตารางแก้ไขข้อมูล
st.subheader(f"📝 รายการเบิกสินค้า")

edited_df = st.data_editor(
    st.session_state.df,
    num_rows="dynamic",
    column_config={
        "Date": st.column_config.DateColumn(
            "วันที่",
            default=datetime.now().date(),
            format="YYYY-MM-DD",
        ),
        "รายการ": st.column_config.TextColumn("ชื่อสินค้า", required=True),
        "ราคา": st.column_config.NumberColumn("ราคาต่อชิ้น", min_value=0, format="%.2f"),
        "จำนวน/ชิ้น": st.column_config.NumberColumn("จำนวน", min_value=0, step=1),
        "รวม/บาท": st.column_config.NumberColumn("รวมเป็นเงิน", disabled=True)
    },
    use_container_width=True,
    key="editor"
)

# ปุ่มบันทึกขึ้น Cloud
if st.button("☁️ บันทึกขึ้น Google Sheets", type="primary"):
    with st.spinner('กำลังบันทึกข้อมูลขึ้น Cloud...'):
        # คำนวณยอดใหม่ก่อนบันทึก
        edited_df['รวม/บาท'] = edited_df['ราคา'] * edited_df['จำนวน/ชิ้น']
        
        # อัปเดต state
        st.session_state.df = edited_df
        
        # บันทึกลง Google Sheets
        # ถ้าชื่อ worksheet (เดือน) ยังไม่มี ระบบจะสร้าง Tab ใหม่ให้เองโดยอัตโนมัติ
        conn.update(worksheet=get_current_sheet_name(), data=edited_df)
        
        st.success("บันทึกเรียบร้อย! ข้อมูลอยู่บน Google Sheets แล้ว")
        st.rerun()

st.divider()

# สรุปยอดรายวัน
st.subheader("📅 สรุปยอดแยกตามวัน")
if not current_df.empty and 'Date' in current_df.columns:
    summary_by_date = current_df.groupby('Date')[['จำนวน/ชิ้น', 'รวม/บาท']].sum().sort_index(ascending=False)
    st.dataframe(summary_by_date)